{
  "default": {
    "paths": ["src/features/**/*.feature"],
    "require": [
      "src/steps/**/*.ts",
      "src/support/**/*.ts"
    ],
    "requireModule": ["ts-node/register"],
    "format": [
      "progress",
      "json:src/reports/cucumber-report.json"
    ],
    "timeout": 60000
  }
}
